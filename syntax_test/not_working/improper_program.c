/*  C is a procedural language, so it requires instructions to be nested in a proper function
This is an example showing an if instruction not nested in a function structure
*/

int a = 1;

if (a > 0 ) printf("a è maggiore di zero");
