#include <stdio.h>

int main(void) 
{
    int cond = 5;

    if ( cond > 5)
    {
    	printf ( "Variable has a value greater then 5\n" );
    }
    else {
        printf ( "Variable has a value lesser then or equal to 5\n" );
    }

    return 0;	
}
